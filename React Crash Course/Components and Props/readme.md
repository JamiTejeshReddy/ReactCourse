Components are any building blocks for any React applications
components can be as small as a button and as large as a page

What are functional components:
FC are sinmple JS fiunctions that returns JSX.

Class components are ES6 classes that extend from React.Components
